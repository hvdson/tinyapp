<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/_header %>
</head>
<body>
    <% for (let key in urls) { %>
    <form method="POST" action="/urls/<%= key %>/delete"><%= key %> -> <a href="<%= urls[key] %>"><%= urls[key] %></a><button type="submit">delete</button></form>
    <%} %>
  
</body>
</html>

<!-- 
<table style="width:50%">
    <tr>
      <th>URL</th>
      <th>Shortened URL</th>
    </tr>
    
    <tr>
      <td>
        
      </td> 
    </tr>
    
  </table> -->